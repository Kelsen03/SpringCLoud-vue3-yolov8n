# 基于 Spring Cloud + Vue 的连锁超市管理微服务系统的设计与实现

## 目录

### 1 引言// src/router/index.js

import { createRouter, createWebHistory } from 'vue-router'

// 1. 定义路由规则
const routes = [
  {
    path: '/login',
    name: 'Login',
    component: () => import('../views/Login.vue'),
    meta: { title: '系统登录' }
  },
  {
    path: '/',
    name: 'Layout',
    component: () => import('../components/Layout.vue'),
    redirect: '/dashboard', // 默认跳转
    children: [
      {
        path: 'dashboard',
        name: 'Dashboard',
        component: () => import('../views/Dashboard.vue'),
        meta: { title: '经营概览', roles: ['HQ', 'STORE'] } // 权限配置
      },
      {
        path: 'product',
        name: 'Product',
        component: () => import('../views/Product.vue'),
        meta: { title: '商品管理', roles: ['HQ', 'STORE'] }
      },
      {
        path: 'inventory',
        name: 'Inventory',
        component: () => import('../views/Inventory.vue'),
        meta: { title: '库存监控', roles: ['HQ', 'STORE'] }
      },
      {
        path: 'order',
        name: 'Order',
        component: () => import('../views/Order.vue'),
        meta: { title: '收银台', roles: ['STORE'] } // 仅门店可见
      },
      {
        path: 'transfer',
        name: 'Transfer',
        component: () => import('../views/Transfer.vue'),
        meta: { title: '调拨补货', roles: ['HQ', 'STORE'] }
      }
    ]
  }
]

const router = createRouter({
  history: createWebHistory(),
  routes
})

// 2. 全局前置守卫：实现登录拦截与权限控制
router.beforeEach((to, from, next) => {
  const token = localStorage.getItem('token')
  const userRole = localStorage.getItem('role')

  // 未登录拦截
  if (to.path !== '/login' && !token) {
    return next('/login')
  }

  // 权限校验
  if (to.meta.roles && userRole) {
    if (!to.meta.roles.includes(userRole)) {
      alert('无权访问该页面')
      return next(from.path)
    }
  }
  
  next()
})

export default router
1.1 研究背景及意义
1.2 研究现状及问题
    1.2.1 连锁零售信息化发展历程
    1.2.2 国内微服务架构应用现状
    1.2.3 国外零售供应链系统研究现状
1.3 本文研究内容
1.4 本文组织结构

### 2 连锁超市微服务管理系统需求分析
2.1 连锁超市业务流程现状调研
2.2 系统整体需求分析
2.3 用户角色分析
    2.3.1 总部管理员（HQ）：统筹全局商品、定价、跨店调拨、数据分析
    2.3.2 门店店长（Store Manager）：门店库存监控、补货申请、本地商品管理
    2.3.3 收银员（Cashier）：前台收银、会员积分、小票打印
2.4 功能性需求分析
    2.4.1 商品档案与权限管理
        - 商品增删改查
        - 统配商品与自营商品权限隔离（isLocal 字段控制）
    2.4.2 分布式库存管理与调拨
        - 多门店库存实时监控
        - 跨店库存调拨（A店 -> B店）
        - 总部批量补货（多选门店、防呆检查）
    2.4.3 订单交易与结算
        - 扫码/输入ID添加商品
        - 实时库存扣减
        - 会员积分抵扣
        - 促销价格计算
    2.4.4 经营数据统计报表
        - 销售额统计（按日/月/门店）
        - 热销商品排行
    2.4.5 基础信息维护管理
2.5 非功能性需求分析
    - 高并发支持（库存扣减）
    - 数据一致性（调拨与商品可见性同步）
    - 系统安全性（JWT 认证）
2.6 本章小结

### 3 连锁超市微服务管理系统设计
3.1 系统整体设计
    3.1.1 微服务技术架构设计（Spring Cloud Alibaba）
    3.1.2 业务功能模块划分（Auth, Product, Inventory, Order, Gateway）
    3.1.3 服务注册与发现架构（Nacos 2.x）
    3.1.4 系统部署架构（前后端分离）
3.2 数据库设计
    3.2.1 数据库选型与分库策略（MySQL 8.0, 独立 Schema 隔离）
    3.2.2 数据库结构设计（ER图）
    3.2.3 核心数据表设计
        - `product` 表（id, name, price, is_local, store_id）
        - `inventory` 表（id, product_id, store_id, stock, production_date）
        - `orders` 表（id, member_id, total_amount, status）
3.3 系统功能详细设计
    3.3.1 统一认证与鉴权设计（JWT + Gateway Filter）
    3.3.2 商品可见性与权限隔离设计（总部可见所有，门店仅见授权商品）
    3.3.3 库存原子扣减与防超卖设计（MyBatis-Plus 乐观锁/SQL原子更新）
    3.3.4 跨店调拨与自动修复机制设计（库存转移 + 商品档案自动同步）
3.4 远程调用与容错设计（OpenFeign + LoadBalancer）
3.5 本章小结

### 4 连锁超市微服务管理系统的实现
4.1 系统开发技术栈
    4.1.1 前端技术栈（Vue 3 + Vite + Element Plus + Axios）
    4.1.2 后端核心技术（Spring Boot + MyBatis-Plus）
    4.1.3 微服务治理组件（Nacos + OpenFeign）
    4.1.4 数据库技术（MySQL + 事务管理）
4.2 系统开发环境搭建
4.3 前端关键功能实现 **（重点补充）**
    4.3.1 基于 Vue Router 的动态路由与角色权限控制
        - 路由守卫 (`router.beforeEach`) 实现未登录拦截
        - 根据角色 (`HQ`/`STORE`) 动态重定向首页
    4.3.2 收银台交互逻辑实现 (`Order.vue`)
        - 商品扫描与实时搜索（防抖优化）
        - 购物车本地状态管理（Reactive State）
        - 促销价格与会员积分的动态计算
    4.3.3 智能补货与调拨前端实现 (`Transfer.vue`)
        - 多门店批量补货逻辑（循环调用 API）
        - 商品ID输入防抖查询 (`debounce` + `watch`)
        - 自动填充商品名称与冲突提示（用户体验优化）
    4.3.4 数据可视化大屏实现 (`Analysis.vue`)
        - ECharts 图表集成
        - 响应式布局适配
4.4 后端核心业务逻辑实现
    4.4.1 用户登录与权限验证实现
    4.4.2 商品管理模块实现（统配与自营隔离逻辑）
    4.4.3 库存管理模块实现
        - `deduct` 接口：原子性扣减库存
        - `replenish` 接口：支持新老商品自动识别更新
    4.4.4 跨店调拨业务实现 **（创新点）**
        - `transfer` 接口逻辑：A店扣减 -> B店增加
        - 数据一致性保障：自动复制生产日期、保质期等元数据
        - 可见性修复：调用 `ensureProductVisibility` 自动升级私有商品为全局商品
    4.4.5 订单交易模块实现（分布式调用与回滚）
4.5 本章小结

### 5 连锁超市微服务管理系统的测试
5.1 测试方法与策略
5.2 测试环境与工具（Postman、JMeter、Chrome DevTools）
5.3 系统功能测试
    5.3.1 商品管理功能测试（增删改查、权限边界）
    5.3.2 库存调拨流程测试（跨店调拨、数据同步验证）
    5.3.3 订单交易流程测试（正常结算、库存不足拦截、积分抵扣）
5.4 系统性能与并发测试
    5.4.1 库存扣减并发压力测试
    5.4.2 服务调用链路稳定性测试
5.5 本章小结

### 6 总结与展望
6.1 总结
    - 成功构建了一套完整的微服务零售系统
    - 解决了跨店调拨中的数据孤岛问题
    - 实现了前后端分离的高效开发模式
6.2 展望
    - 引入 Redis 缓存进一步提升热点商品性能
    - 引入 Seata 实现分布式事务强一致性
    - 移动端 App/小程序扩展

### 参考文献
