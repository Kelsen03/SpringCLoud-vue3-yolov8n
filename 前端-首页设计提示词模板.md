# AI 营销首页生成提示词模板

> 将下面这段发给 Claude Code，可以快速生成类似风格的营销介绍页面。
> 替换 `[ ]` 中的内容为你项目的具体信息。

---

## 完整提示词

```
我有一个 [Vue3/React] 项目，需要在首页做一个全屏滚动的营销介绍页面。
功能页面的代码不要动，只改首页。

参考设计风格：sofiapapadopoulou.com — 沉浸式暗调叙事、GSAP 丝滑滚动、光标交互、高级设计感。

具体要求：

【主题与配色】
- 使用统一的深色调背景（#XXXXXX），所有 Section 背景一致，不要白→暗的割裂感
- 字体要有透明感，可见但不刺眼
- 点缀色用柔和的蓝色（#XXXXXX），不要荧光蓝

【排版布局】
- Hero 大标题：超大字号紧贴左边，默认极暗，光标靠近时以光标为圆心径向发光
- 功能介绍区三栏：标题贴左 → 示意图居中 → 描述文字靠右
- 模块卡片区：4 列网格，hover 上浮 + 渐变光晕

【滚动动画】
- 集成 Lenis 平滑滚动（duration 2.0，缓出 easing）
- GSAP ScrollTrigger 驱动逐元素揭示：文字淡入，图片滑入+缩放
- 不要突兀的弹入动画，要自然而从容

【交互效果】
- 标题发光：使用 background-clip: text + radial-gradient，以光标为圆心，中心亮白向外衰减到可见暗色
- 自定义光标：柔和光环，慢悠悠跟随系统光标，静止时合体
- 技术关键词链接：默认虚线下划线，hover 弹出描述气泡，click 跳转外部文档

【背景特效】
- Canvas 多层正弦海浪线，相位随滚动位置动态变化
- 颜色与背景同系但稍亮，极低透明度

【序幕动画】
- 进入页面前先全黑底 + 两段文字 GSAP 淡入淡出
- 序幕结束后再显示主页内容

【移动端】
- 768px 断点，光标隐藏，海浪隐藏
- 三栏折叠为单列
- 禁用 scroll-snap

【弹窗】
- 保留现有登录/注册弹窗，3D 翻转效果不动

我还要一个右下角调色盘（开发时用），能实时改背景色，确认颜色后再删掉打包。
```

---

## 逐项解释 — 方便你下次按需组合

### 1. 主题色
```
帮我试 XXXX 蓝 #XXXXXX 做主题背景
```
每次只需换一个 hex 值，AI 会全局替换。

### 2. 标题发光（核心效果）
```
大标题默认暗色，光标靠近时以光标为圆心发光照亮周围字母
```
实现原理：`background: radial-gradient(circle at cursor, bright, dim)` + `background-clip: text`。关键：渐变需覆盖整个文字区域（从亮到可见暗色），不能直接到 transparent，否则远处字母会消失。

### 3. 平滑滚动
```
集成 Lenis 平滑滚动，让滚轮像丝绸一样
```
Lenis 的 key 参数：`duration: 2.0`（越大越慵懒）、`wheelMultiplier: 0.75`（越小滚轮越"重"）。

### 4. 滚动揭示动画
```
用 GSAP ScrollTrigger 替代 CSS 动画，逐元素按序淡入
```
注意：滚动容器是 div 时，ScrollTrigger 必须指定 `scroller` 参数，否则默认监听 window。

### 5. Canvas 海浪
```
三层正弦浪线，随滚动位移改变相位
```
公式：`y = baseY + sin(x * freq + scrollPhase) * amp`。多频率叠加产生自然感。

### 6. 调色盘
```
右下角做一个实时调色盘，能改背景色
```
用 `v-model` + `<input type="color">`，`applyPalette` 函数遍历 DOM 修改所有 section 背景。

### 7. 技术链接
```
鼠标放上去展开描述气泡，点击跳转外部文档
```
用 `data-url` / `data-desc` 属性存储链接和描述，`::after` 伪元素渲染气泡。

### 8. 自定义光标
```
柔和光环光标，不紧贴系统光标，慢悠悠跟在后面
```
JS `requestAnimationFrame` + `lerp 0.06` 平滑跟随，静止时直接吸附。

---

## 关键依赖

```json
{
  "gsap": "^3.15",
  "@studio-freight/lenis": "^1.0"
}
```

## 常见坑

| 问题 | 原因 | 解决 |
|------|------|------|
| 滚动没动画 | ScrollTrigger 未指定 scroller | 传 `scroller: scrollContainer.value` |
| 发光文字远处消失 | gradient 终点设了 transparent | 终点用可见暗色 rgba |
| hover 文字跳动 | padding 变化挤了布局 | 不用 padding 过渡，改用 text-shadow |
| 背景不统一 | 各 section 独立设了 bg | 全部用同一个 hex 值 |
| 序幕时光标卡左上角 | 光标 div 在帷幕前就渲染了 | `v-show="!introActive"` 仅在序幕后显示 |
